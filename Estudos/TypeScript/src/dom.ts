const campoNome = document.getElementById('nome'); //type = HTMLElement | null
const formulario = document.getElementsByTagName('form'); //type HTMLCollectionOf<HTMLFormElements> basicamente um array de elementos HTML
const botaoEnviar = document.getElementById('btn-enviar'); //type HTMLElement | null

botaoEnviar?.addEventListener('click', (e: MouseEvent) => {

}) 

